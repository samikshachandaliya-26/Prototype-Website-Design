
  # Prototype Website Design

  This is a code bundle for Prototype Website Design. The original project is available at https://www.figma.com/design/fdie0b8MV1seGEAnMkXWfK/Prototype-Website-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  